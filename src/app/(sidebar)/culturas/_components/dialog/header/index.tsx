import { DialogHeader, DialogTitle } from "@/components/ui/dialog";

export function Header() {
  return (
    <DialogHeader>
      <DialogTitle>Adicionar cultura</DialogTitle>
    </DialogHeader>
  );
}